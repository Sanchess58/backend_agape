# HTTP сервер + проверка для Certbot
server {
    listen 80;
    server_name house-agape.ru www.house-agape.ru;

    # Важно для Let's Encrypt
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
    }

    # Проксирование на backend
    location / {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS сервер (будет активен после получения сертификата)
# server {
#     listen 443 ssl http2;
#     server_name house-agape.ru www.house-agape.ru;
#     
#     ssl_certificate /etc/letsencrypt/live/house-agape.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/house-agape.ru/privkey.pem;
#     
#     location / {
#         proxy_pass http://backend:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
# }